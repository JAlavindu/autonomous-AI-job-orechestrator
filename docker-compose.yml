x-common-env: &common-env
  REDIS_HOST: redis
  DATABASE_URL: postgresql+psycopg2://orchestrator:orchestrator@postgres:5432/orchestrator
  DEFAULT_TENANT_ID: 00000000-0000-0000-0000-000000000001
  DEFAULT_TENANT_NAME: default
  SCHEDULER_MODE: heuristic
  EXECUTOR_ALLOWLIST: sleep
  LOG_LEVEL: INFO
  RL_MODEL_PATH: ai_brain.pth
  JOB_STREAM_KEY: stream:jobs
  JOB_STREAM_GROUP: workers
  JOB_STREAM_BLOCK_MS: 5000
  JOB_STREAM_MAXLEN: 10000

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: orchestrator
      POSTGRES_PASSWORD: orchestrator
      POSTGRES_DB: orchestrator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # --- Dev (default): bind-mount source for hot reload ---
  api:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres

  worker:
    build: .
    command: python src/orchestrator/worker.py
    volumes:
      - .:/app
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres
      - api

  dashboard:
    build: .
    command: streamlit run src/dashboard/app.py --server.port 8501 --server.address 0.0.0.0
    volumes:
      - .:/app
    ports:
      - "8501:8501"
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres

  # --- Prod profile: immutable image, no bind mounts ---
  api-prod:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres
    profiles:
      - prod

  worker-prod:
    build: .
    command: python src/orchestrator/worker.py
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres
      - api-prod
    profiles:
      - prod

  dashboard-prod:
    build: .
    command: streamlit run src/dashboard/app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    environment:
      <<: *common-env
    depends_on:
      - redis
      - postgres
    profiles:
      - prod

volumes:
  redis_data:
  postgres_data:
